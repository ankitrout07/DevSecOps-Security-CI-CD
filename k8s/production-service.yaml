apiVersion: v1
kind: Service
metadata:
  name: final-production-service
spec:
  type: NodePort
  selector:
    app: node-app
    variant: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
